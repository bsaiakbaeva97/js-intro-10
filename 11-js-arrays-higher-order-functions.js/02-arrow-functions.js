function average(num1, num2) {
    return (num1 + num2) / 2
}

console.log(average(5, 4));

// Function Expression

const average = function (a, b) {
    return (num1 + num2) / 2;
}
// Arrow function
const average = (a, b) => {
    return (num1 + num2) / 2;
}